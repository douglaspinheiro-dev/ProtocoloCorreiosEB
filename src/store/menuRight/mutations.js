
/*
export const someMutation = (state) => {}
 */
/*
export const setLinks = (state, links) => {
  state.links = links
}

export const setBotaoListagemDeRegistros = (state, valor) => {
  state.botaoListagemDeRegistros = valor
}

export const setListagemDeRegistros = (state, valor) => {
  state.listagemDeRegistros = valor
}

export const setRegistros = (state, registros) => {
  state.registros = registros
}

export const alteraInicioDaLista = (state, inicio) => {
  state.inicio = inicio
}

export const alteraFimDaLista = (state, fim) => {
  state.fim = fim
}

export const adicionaRegistroNaLista = (state, valor) => {
  state.registros.push(valor)
}

export const alteraUnicoRegistro = (state, registro) => {
  state.registros.forEach(function (elemento, index) {
    if (elemento.valor === Number(registro.valor)) {
      elemento.posicao = index
      elemento.label = registro.label
      elemento.subLabel = registro.subLabel
    }
  })
}

export const removeRegistro = (state, registro) => {
  // let idRegistro = state.registros.filter(registro => registro.registro === valor)
  state.registros.splice(registro, 1)
  // let registroRemovido = lodash.remove(state.registros, registro => registro.valor === valor)
  // console.log(registroRemovido[0])
}
*/
