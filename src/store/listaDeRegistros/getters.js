export const getRegistros = (state) => {
  return state.registros
}

export const getInicio = (state) => {
  return state.inicio
}

export const getFim = (state) => {
  return state.fim
}

export const getBusca = (state) => {
  return state.busca
}
